async function drawGame(gameSession) {
    if (gameSession.gameStatus === 'ONGOING') {
        // const depo = gameSession.depo;
        // const portals = depo.portals;
        // for (const portal of portals) {
        //     console.log(portal.cityId);
        //     console.log(portal.backImage);
        //     console.log(staticData[portal.faceImage]);
        //     console.log(staticData[portal.cityName]);
        // }
    }
}